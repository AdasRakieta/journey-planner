# SmartHome - Environment Variables for Portainer Stack
# Copy these to Portainer → Stacks → smarthome → Environment variables

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
IMAGE_TAG=latest
FLASK_ENV=production

# UWAGA: Wygeneruj własny SECRET_KEY!
# Python: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here_generate_new_one

# ===========================================
# DATABASE CONFIGURATION (PostgreSQL)
# ===========================================
# UWAGA: Zastąp własnymi credentials!
DB_HOST=192.168.1.218
DB_PORT=5432
DB_NAME=smarthome_db
DB_USER=smarthome_user
DB_PASSWORD=your_db_password_here

# ===========================================
# EMAIL CONFIGURATION (SMTP)
# ===========================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=smarthome.alertmail@gmail.com
SMTP_PASSWORD=pqvg eabu bmka mggk
ADMIN_EMAIL=szymon.przybysz2003@gmail.com

# ===========================================
# REDIS CONFIGURATION (Internal)
# ===========================================
REDIS_HOST=smarthome_redis_standalone
REDIS_PORT=6379

# ===========================================
# URL PREFIXES (Empty for Traefik)
# ===========================================
# Traefik automatycznie dodaje /smarthome prefix
URL_PREFIX=
API_PREFIX=
STATIC_PREFIX=
SOCKET_PREFIX=

# ===========================================
# ASSET VERSIONING (Optional)
# ===========================================
ASSET_VERSION=
